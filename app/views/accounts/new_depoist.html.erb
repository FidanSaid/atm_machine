<%= form_for  @account, :url => create_deposit_account_path, method: :post do |f| %>
 
 <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
  </br>
<%=link_to'Back',atm_mach_path(session[:atm_no]) %>

<% end %>